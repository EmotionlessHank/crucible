# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 数据与产物(绝不入库 — 体积大且可重新下载)
data/
mlruns/
journal/
registry/
*.parquet
*.feather
*.csv
*.sqlite
*.db

# 密钥与实盘配置(绝不入库 — 资金安全红线)
.env
.env.*
*.key
*.pem
config/config.live.json
**/secrets*
**/*credential*

# OMC / 工具状态
.omc/

# 本地中文过程笔记(不上传)
.progress/

# git worktree 隔离开发目录(worktree-dev skill 使用)
.worktrees/

# 本地个人权限配置(不上传;worktree 里软链回主工作区)
.claude/settings.local.json

# OS / 编辑器
.DS_Store
.idea/
.vscode/
